% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{filter_by_year_month}
\alias{filter_by_year_month}
\title{Filter a data frame to an inclusive year/month range}
\usage{
filter_by_year_month(dataset, y_s, y_e, m_s = NULL, m_e = NULL)
}
\arguments{
\item{dataset}{A data frame with \code{year} and \code{month} columns.}

\item{y_s}{Start year (integer), or a \code{Date} marking the start of the range.}

\item{y_e}{End year (integer), or a \code{Date} marking the end of the range.}

\item{m_s}{Start month (integer); ignored if \code{y_s} is a \code{Date}.}

\item{m_e}{End month (integer); ignored if \code{y_e} is a \code{Date}.}
}
\value{
The filtered data frame.
}
\description{
Keeps rows of \code{dataset} (which must have \code{year} and \code{month} columns)
between a start and end point, inclusive. The range can be given either
as \code{year}/\code{month} integers, or as \code{Date} objects for \code{y_s}/\code{y_e} (in
which case \code{m_s}/\code{m_e} are derived automatically and any explicit month
arguments are ignored).
}
