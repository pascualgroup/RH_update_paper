% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{prediction_by_window}
\alias{prediction_by_window}
\title{Forecast forward year by year, refitting on a sliding window}
\usage{
prediction_by_window(
  parameters,
  city,
  n_sims = 1,
  model = "original",
  covariate = "RH2",
  start_year = 1997,
  start_month = 1,
  end_year = 2014,
  end_month = 12,
  end_year_prediction = 2019,
  end_month_prediction = 12,
  prediction_window,
  window_start,
  window_end,
  parameters_to_fit,
  dataset,
  output_format = c("data.frame", "pomps"),
  ignore_covid = TRUE,
  actual_end_year = NULL,
  actual_end_month = NULL
)
}
\arguments{
\item{parameters}{Named numeric vector or single-row data.frame/tibble
with starting parameter values for the initial training simulation and
first refit window.}

\item{city}{The name of the city (currently unused inside the function
body, kept for interface consistency with related functions).}

\item{n_sims}{Number of particles/simulations used for the training
simulation, particle filters, \code{mif2} fitting, and forecast simulations.}

\item{model}{The model to use (default is "original").}

\item{covariate}{The covariate to use (default is "RH2").}

\item{start_year}{The starting year of the training period (default is 1997).}

\item{start_month}{The starting month of the training period (default is 1).}

\item{end_year}{The ending year of the training period (default is 2014).}

\item{end_month}{The ending month of the training period (default is 12).}

\item{end_year_prediction}{The ending year of the forecast horizon (default is 2019).}

\item{end_month_prediction}{The ending month of the forecast horizon (default is 12).}

\item{prediction_window}{Integer. Length, in years, of the sliding
refitting window used before each forecast year (see
\code{separate_pfilter_simulation_datasets()}).}

\item{window_start}{The starting month of the window used to aggregate
the covariate within each year.}

\item{window_end}{The ending month of the window used to aggregate the
covariate within each year.}

\item{parameters_to_fit}{Character vector of parameter names that are
allowed to move (given a random-walk standard deviation) during the
yearly \code{mif2} refits; all other parameters are held fixed at their
current value.}

\item{dataset}{The path to the dataset CSV file.}

\item{output_format}{The format of the output (default is "data.frame").
Currently validated but not forwarded to the internal \code{simulate()}
calls, which always request \code{"data.frame"}.}

\item{ignore_covid}{Logical. If \code{TRUE} (default), \code{PF} observations for
the year 2020 are set to \code{NA} before fitting/simulating, excluding the
COVID-19 disruption period from influencing the model.}

\item{actual_end_year}{Optional. End year used for covariate
normalization range in \code{preprocess_data_prediction()}; defaults to
\code{end_year} when \code{NULL}.}

\item{actual_end_month}{Optional. End month used for covariate
normalization range in \code{preprocess_data_prediction()}; defaults to
\code{end_month} when \code{NULL}.}
}
\value{
A list with elements:
\itemize{
\item \code{training}: a list with the training-period simulation.
\item \code{prediction}: a list of simulated forecasts, one per forecast year.
\item \code{parameters_prediction}: a list of fitted parameter vectors, one per
forecast year, plus the original \code{training} starting parameters.
\item \code{ignore_covid}: the \code{ignore_covid} argument, echoed back.
\item \code{complete_data}: the full training + prediction data (with COVID masking
applied if requested).
}
}
\description{
Used for the window-length sensitivity analysis: starting from a training
fit, walks forward one forecast year at a time through
\code{end_year_prediction}, and before each forecast year re-estimates hidden
states and (for the parameters in \code{parameters_to_fit}) refits parameters
via \code{mif2} on a trailing window of \code{prediction_window} years of data.
Concretely, for each step it: (1) particle-filters over the current
\code{prediction_window}-year window to re-seed initial compartment states
(\code{pfilter_initial_states()}); (2) runs \code{pomp::mif2()} over that
same window, with a random-walk applied only to \code{parameters_to_fit}
(other parameters are fixed via \code{generate_rw_sd(..., profile_var = ...)});
(3) re-estimates initial states again with the newly fit parameters; and
(4) simulates the next 12-month forecast year with those parameters and
states. If \code{ignore_covid = TRUE}, \code{PF} is masked to \code{NA} for the year 2020
before windowing/fitting, so that period does not influence the fits.
}
