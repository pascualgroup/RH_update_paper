% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{prepare_model}
\alias{prepare_model}
\title{Prepare data and a POMP model from CSV inputs}
\usage{
prepare_model(
  parameters,
  dataset,
  covariate,
  start_year,
  end_year,
  start_month,
  end_month,
  window_start,
  window_end,
  actual_end_year = NULL,
  actual_end_month = NULL,
  extra_cov = NULL,
  normalize_extra = TRUE,
  model,
  model_dir = "dynamical_model/fitting",
  model_file = NULL,
  validate_only = FALSE
)
}
\arguments{
\item{parameters}{Named numeric vector or single-row data.frame/tibble with
starting parameter values (when a data.frame the first row is used).}

\item{covariate}{Character. Column name in the covariate CSV to use as the
primary covariate.}

\item{start_year, end_year, start_month, end_month}{Integers specifying the
inclusive time window to prepare.}

\item{extra_cov}{Optional character vector of additional covariate column
names to include from \code{extra_cov_dataset} or \code{dataset_VC}.}

\item{normalize_extra}{Logical. Whether to normalize extra covariates with
a safe min-max transform.}

\item{model}{Character short name used to locate \verb{pomp_object_<model>.R}
inside \code{model_dir} (ignored when \code{model_file} is provided).}

\item{model_dir}{Directory where model definition files live.}

\item{model_file}{Optional absolute path to a \code{pomp_object_*.R} file to
source instead of using \code{model} + \code{model_dir}.}

\item{validate_only}{Logical; if \code{TRUE} performs validations and returns a
small list with \code{status = 'ok'} rather than building the full \code{pomp}
object. Useful for CI or dry-run checks.}

\item{dataset_mal}{Path to CSV file with malaria case data (will be read
using \code{readr::read_csv}). The resulting data must contain \code{time} or
\code{time_end} and \code{PF} after preprocessing.}

\item{dataset_VC}{Path to CSV file with covariate data (VC/climate).}

\item{extra_cov_dataset}{Optional path to a CSV containing additional
covariates (if not present in \code{dataset_VC}).}
}
\value{
A list with elements:
\itemize{
\item \code{po}: a \code{pomp} object (or a small validation list when \code{validate_only=TRUE})
\item \code{dat}: preprocessed covariate table (includes \code{time}, \code{pop}, \code{dpopdt})
\item \code{dat_mal}: preprocessed malaria observation table with columns \code{time} and \code{PF}.
}
}
\description{
Convenience wrapper that reads malaria and covariate CSV files, runs
\code{preprocess_data()} to produce covariate and observation tables, coerces
\code{parameters} into a named numeric vector, and calls
\code{prepare_pomp_model()} to build the final \code{pomp} object.
}
\details{
This helper is intended for interactive and programmatic workflows where
the raw inputs are CSV files (paths) rather than already-loaded data
frames.
}
\seealso{
\code{preprocess_data}, \code{prepare_pomp_model}, \code{fit_model}
}
