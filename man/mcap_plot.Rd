% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcap.R
\name{mcap_plot}
\alias{mcap_plot}
\title{Plot an MCAP profile with its confidence interval}
\usage{
mcap_plot(mcap_output, font_size = 16)
}
\arguments{
\item{mcap_output}{A list as returned by \code{\link[=mcap]{mcap()}} / \code{\link[=simple_mcap]{simple_mcap()}} /
\code{\link[=mcap_checked]{mcap_checked()}}, containing at least \code{parameter}, \code{lp}, \code{fit}, \code{ci},
and \code{delta}.}

\item{font_size}{Base font size passed to \code{theme_bw()}.}
}
\value{
A ggplot object showing the loess-smoothed profile (blue line),
the raw profile log-likelihood points, the maximizing point
(highlighted in green), the confidence-interval bounds (vertical
dashed lines), and the log-likelihood cutoff used to define them
(horizontal line). The plot title reports the MLE and CI bounds.
}
\description{
Builds a diagnostic plot of a profile log-likelihood together with its
MCAP smoothing and confidence interval, as returned by \code{\link[=mcap]{mcap()}},
\code{\link[=simple_mcap]{simple_mcap()}}, or the concave case of \code{\link[=mcap_checked]{mcap_checked()}}.
}
\details{
Infinite CI bounds (as can occur when the cutoff is never
crossed within the explored grid) are replaced with the min/max of the
observed parameter values so the plot always has finite limits.
}
