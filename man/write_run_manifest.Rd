% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_manifest.R
\name{write_run_manifest}
\alias{write_run_manifest}
\title{Write a run manifest to disk atomically}
\usage{
write_run_manifest(path, info, pretty = FALSE, append = FALSE)
}
\arguments{
\item{path}{Character scalar. Path to the target JSON file to create or
append to.}

\item{info}{List. A manifest information structure (typically created with
\code{create_manifest_info()}) that will be written as JSON.}

\item{pretty}{Logical scalar. If \code{TRUE} the JSON is pretty-printed.}

\item{append}{Logical scalar. If \code{TRUE} and \code{path} exists, attempt to read
the existing JSON and append \code{info} to it. If \code{FALSE} the file will be
overwritten.}
}
\value{
Invisibly returns \code{path} (the file path written).
}
\description{
This function writes a run manifest (an R list) to a JSON file specified by
\code{path}. The write is performed atomically by writing to a temporary file in
the same directory and then renaming it into place. When \code{append = TRUE} and
the target file already exists, the function attempts to read the existing
JSON and append the new \code{info} entry. Existing single-object manifests are
converted to an array before appending.
}
