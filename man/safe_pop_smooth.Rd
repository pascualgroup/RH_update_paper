% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{safe_pop_smooth}
\alias{safe_pop_smooth}
\title{Smooth a population time series and its derivative}
\usage{
safe_pop_smooth(time, pop, method = "smooth.spline", params = list(spar = 0.5))
}
\arguments{
\item{time}{Numeric vector of time points.}

\item{pop}{Numeric vector of population sizes, same length as \code{time}.}

\item{method}{Character. Smoothing method to use. Only \code{"smooth.spline"}
is currently supported.}

\item{params}{List of parameters for the smoothing method (currently
unused for \code{"smooth.spline"}, which is applied with its defaults after
an initial loess fit).}
}
\value{
A list with elements \code{pop} (smoothed population) and \code{dpopdt}
(its first derivative).
}
\description{
Fits a smooth curve to a population size series and returns both the
smoothed values and their first derivative with respect to time (used
downstream as the \code{dpopdt} covariate). Falls back to returning the raw
population with \code{dpopdt = NA} when there are too few points (fewer than 5
observations, or fewer than 3 distinct non-missing values) to smooth
reliably.
}
\keyword{internal}
