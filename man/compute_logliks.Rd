% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{compute_logliks}
\alias{compute_logliks}
\title{Compute particle-filter log-likelihoods for a set of parameter vectors}
\usage{
compute_logliks(
  po,
  parameters,
  pfilter_control = NULL,
  allow_parallel = FALSE,
  n_cores = 1,
  seed = TRUE,
  chunk.size = NULL,
  scheduling = 1,
  verbose = FALSE
)
}
\arguments{
\item{po}{A \code{pomp} object (e.g. from \code{prepare_pomp_model()}).}

\item{parameters}{A data.frame/tibble with one row per parameter set to
evaluate (columns are parameter names).}

\item{pfilter_control}{Optional list with \code{Nrep} (replicates per
parameter row) and \code{Np} (number of particles), passed to
\code{pomp::pfilter()}. Defaults to \code{list(Nrep = 10, Np = 10000)}.}

\item{allow_parallel}{Logical; if \code{TRUE} sets a \code{multicore} future plan
with \code{n_cores} workers, otherwise runs sequentially.}

\item{n_cores}{Integer. Number of workers to use when \code{allow_parallel = TRUE}.}

\item{seed}{Logical or integer seed forwarded to \code{foreach}'s
\code{.options.future}.}

\item{chunk.size}{Optional chunk size forwarded to \code{foreach}'s
\code{.options.future}.}

\item{scheduling}{Numeric scheduling parameter forwarded to \code{foreach}'s
\code{.options.future}.}

\item{verbose}{Logical. If \code{TRUE}, emit warnings on individual \code{pfilter}
failures.}
}
\value{
\code{parameters} with two additional columns, \code{loglik} (mean
log-likelihood across replicates) and \code{loglik.se} (its standard error).
}
\description{
For each row of \code{parameters}, runs \code{pfilter_control$Nrep} repeated
\code{pomp::pfilter()} evaluations of \code{po} (optionally in parallel across
\code{future} workers) and summarizes the replicate log-likelihoods per
parameter row via \code{pomp::logmeanexp()}. The caller's \code{future} plan is
preserved and restored on exit.
}
\keyword{internal}
