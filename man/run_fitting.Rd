% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{run_fitting}
\alias{run_fitting}
\title{Run MIF2 fitting and post-process results}
\usage{
run_fitting(
  po,
  n_cores,
  parameters,
  seed_num,
  rdd,
  allow_parallel,
  start_index,
  output_to_file,
  output_result,
  output_log,
  output_format,
  mif_control = list(Np = 3000, Nmif = 50, cooling.type = "geometric",
    cooling.fraction.50 = 0.5),
  pfilter_control = list(Nrep = 10, Np = 10000),
  verbose = FALSE
)
}
\arguments{
\item{po}{A \code{pomp} object prepared by \code{prepare_pomp_model()}.}

\item{n_cores}{Integer number of worker processes to use when \code{allow_parallel = TRUE}.}

\item{parameters}{A list of starting parameter vectors (format expected by \code{pomp::mif2}).}

\item{seed_num}{Integer RNG seed (passed externally for reproducibility).}

\item{rdd}{A \code{pomp::rw_sd} object defining parameter random-walk sizes.}

\item{allow_parallel}{Logical; if \code{TRUE} will set a parallel future plan (multicore).}

\item{start_index}{Integer offset added to sample indices (useful for batched grids).}

\item{output_to_file}{Logical; when \code{TRUE} results are appended to \code{output_result}.}

\item{output_result}{Path to the CSV file to append results.}

\item{output_log}{Path to a log file (currently unused but reserved for future logging).}

\item{output_format}{Character; currently \code{"data.frame"} is supported.}
}
\value{
A data.frame with fitted parameter estimates, log-likelihood summaries and a \code{flag} column indicating successful evaluations (1) or failures (0).
}
\description{
Low-level worker that executes \code{pomp::mif2} on a prepared \code{pomp}
object and post-processes per-sample \code{pfilter} + \code{coef} summaries. The
function preserves and restores the user's \code{future} plan when switching
between parallel and sequential execution.
}
\details{
The function internally calls \code{pomp::mif2} and then runs
\code{pomp::pfilter} for each MIF sample to estimate log-likelihoods. It
uses \code{tryCatch} around heavy operations so single-sample failures become NA
rows instead of aborting the whole batch.
}
