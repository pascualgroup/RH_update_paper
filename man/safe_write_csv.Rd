% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_safe.R
\name{safe_write_csv}
\alias{safe_write_csv}
\title{Atomically write a data.frame to CSV}
\usage{
safe_write_csv(
  df,
  dir = "results",
  prefix = "run",
  suffix = NULL,
  row.names = FALSE,
  ...
)
}
\arguments{
\item{df}{A data.frame or tibble to be written.}

\item{dir}{Directory where the file will be written (created if missing).}

\item{prefix}{File name prefix. The final filename will be \code{paste0(prefix, "_", suffix, ".csv")}.}

\item{suffix}{Optional suffix to use instead of the SLURM-derived suffix.}

\item{row.names}{Passed to \code{write.csv()}; default FALSE.}

\item{...}{Additional arguments passed to \code{utils::write.csv()}.}
}
\value{
Invisibly returns the path to the final CSV file written.
}
\description{
Write a data.frame or tibble to a CSV file atomically by first writing to a
temporary file in the same directory and then renaming it to the final
filename. The final filename is constructed from \code{prefix} and a unique
suffix that by default encodes SLURM environment information (so array
tasks don't collide).
}
