% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{plot_prediction}
\alias{plot_prediction}
\title{Plot a training/prediction trajectory against observed data}
\usage{
plot_prediction(
  dataset,
  mode = c("ribbon", "traj"),
  CI = 0.8,
  font_size = 12,
  by_pop = FALSE,
  pop_division = 1000
)
}
\arguments{
\item{dataset}{A data.frame produced by \code{predict_model()} or
\code{predict_model_states()}, with \code{time}, \code{PF}, \code{pop}, and \code{type} columns.}

\item{mode}{Character. \code{"ribbon"} plots a \code{CI}-width credible interval via
\code{ggdist::stat_lineribbon()}; \code{"traj"} plots individual simulated lines.}

\item{CI}{Numeric. Width of the credible interval used in \code{"ribbon"} mode.}

\item{font_size}{Numeric. Base font size for the plot theme.}

\item{by_pop}{Logical. If \code{TRUE} (ribbon mode only), plots cases per
\code{pop_division} people (\code{PF / (pop / pop_division)}) instead of raw counts.}

\item{pop_division}{Numeric. Population denominator used when \code{by_pop = TRUE}
(e.g. \code{1000} for cases per 1,000 people).}
}
\value{
A \code{ggplot} object.
}
\description{
Takes output from \code{predict_model()}/\code{predict_model_states()} (a long data
frame with \code{time}, \code{PF}, \code{pop}, and a \code{type} column of \code{"data"},
\code{"training"}, \code{"prediction"}) and plots either a credible-interval ribbon
or individual trajectory lines, with a dashed vertical line marking the
end of the training period. Colors distinguish data, training, and
prediction series.
}
