% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcap.R
\name{simple_mcap}
\alias{simple_mcap}
\title{Compute an MCAP profile using a classical, non-Monte-Carlo-adjusted cutoff}
\usage{
simple_mcap(lp, parameter, confidence = 0.95, lambda = 0.75, Ngrid = 1000)
}
\arguments{
\item{lp}{Numeric vector of profile log-likelihood values.}

\item{parameter}{Numeric vector of parameter values corresponding to
\code{lp} (same length).}

\item{confidence}{Confidence level for the interval.}

\item{lambda}{Loess span, and fraction of points (nearest to the
smoothed maximum) used to weight the local quadratic fit.}

\item{Ngrid}{Number of points in the grid used to evaluate the loess
smooth and locate the confidence interval.}
}
\value{
A list with the same elements as \code{\link[=mcap]{mcap()}}: \code{lp}, \code{parameter},
\code{confidence}, \code{quadratic_fit}, \code{quadratic_max}, \code{smooth_fit}, \code{fit},
\code{mle}, \code{ci}, \code{delta}, \code{se_stat}, \code{se_mc}, \code{se}.
}
\description{
A variant of \code{\link[=mcap]{mcap()}} that smooths and fits the local quadratic the same
way, but ignores the Monte Carlo variance term when defining the
confidence interval. Use this when the profile is believed to be
essentially noise-free (e.g. profile points already well-converged) and
a plain likelihood-ratio cutoff is preferred over the MC-inflated one.
}
\details{
Differs from \code{\link[=mcap]{mcap()}} in two ways. First, the loess smoother is
fit with \code{loess.control(surface = "direct")}, computing exact
predictions instead of the (faster, approximate) default interpolation
surface. Second, and more importantly, \code{delta} is set to the plain
\code{qchisq(confidence, df = 1) / 2} cutoff rather than \code{\link[=mcap]{mcap()}}'s
MC-inflated cutoff — \code{se_mc} and \code{se_stat} are still computed and
returned, but \code{se_mc} is not folded into \code{delta}, so the resulting
\code{ci} is a classical profile-likelihood interval that does not account
for Monte Carlo noise in \code{lp}.
}
