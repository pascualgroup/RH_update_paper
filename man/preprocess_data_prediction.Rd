% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{preprocess_data_prediction}
\alias{preprocess_data_prediction}
\title{Preprocess data into separate training and prediction tables}
\usage{
preprocess_data_prediction(
  dat,
  model,
  covariate,
  start_year,
  end_year,
  start_month,
  end_month,
  end_year_prediction,
  end_month_prediction,
  window_start,
  window_end,
  actual_end_year,
  actual_end_month,
  extra_cov = NULL
)
}
\arguments{
\item{dat}{The input data frame containing the raw data to preprocess.}

\item{model}{The model type to be used for preprocessing (controls how
the windowed covariate is computed downstream).}

\item{covariate}{The name of the covariate variable in the data frame.}

\item{start_year}{The start year of the training period.}

\item{end_year}{The end year of the training period.}

\item{start_month}{The start month (in \code{start_year}) of the training period.}

\item{end_month}{The end month (in \code{end_year}) of the training period.}

\item{end_year_prediction}{The end year of the prediction period.}

\item{end_month_prediction}{The end month (in \code{end_year_prediction}) of
the prediction period.}

\item{window_start}{The start month of the window used to aggregate the
covariate within each year.}

\item{window_end}{The end month of the window used to aggregate the
covariate within each year.}

\item{actual_end_year}{The end year used to compute the covariate's
normalization range (min/max/mean/sd); defaults to \code{end_year} when \code{NULL}.}

\item{actual_end_month}{The end month used to compute the covariate's
normalization range; defaults to \code{end_month} when \code{NULL}.}

\item{extra_cov}{Optional data frame describing additional covariates to
merge in, with columns \code{covariate}, \code{window_start}, and \code{window_end}
(one row per extra covariate); each is min-max normalized over the
\code{start_year}/\code{start_month}-\code{actual_end_year}/\code{actual_end_month} range.}
}
\value{
A list of two data frames: \code{dat_training} (training period) and
\code{dat_prediction} (the period immediately following training, through
\code{end_year_prediction}-\code{end_month_prediction}).
}
\description{
Variant of \code{preprocess_data()} used for out-of-sample forecasting. Builds
the smoothed population/derivative and windowed covariate as in
\code{preprocess_data()}, but normalizes the covariate using the min/max/mean/sd
computed over the (possibly longer) \code{actual_end_year}/\code{actual_end_month}
range rather than the \code{end_year}/\code{end_month} training range, so that
training and prediction covariates share a consistent scale. The result is
split into a training table (\code{start_year}-\code{start_month} to
\code{end_year}-\code{end_month}) and a prediction table (the month after
\code{end_year}-\code{end_month} through \code{end_year_prediction}-\code{end_month_prediction}).
}
