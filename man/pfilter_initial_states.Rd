% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{pfilter_initial_states}
\alias{pfilter_initial_states}
\title{Re-seed a parameter vector's initial states from a particle filter}
\usage{
pfilter_initial_states(pomp_model, param, n_sims)
}
\arguments{
\item{pomp_model}{A \code{pomp} object to particle-filter.}

\item{param}{Named numeric parameter vector to pass to \code{pfilter()} and to
update with the re-estimated initial states.}

\item{n_sims}{Integer. Number of particles (\code{Np}) used by the particle
filter.}
}
\value{
A named numeric parameter vector (the fitted coefficients from
\code{pfilter()}) with its \verb{*_0} initial-condition entries replaced by the
particle-filter-estimated states.
}
\description{
Runs \code{pomp::pfilter()} on \code{pomp_model} with \code{save.states = TRUE},
averages the final saved particle states across particles, normalizes the
epidemiological compartments (\code{S1}, \code{E}, \code{I1}, \code{I2}, \code{S2}) to sum to one,
and writes the result into the \verb{*_0} initial-condition slots (\code{S1_0},
\code{E_0}, \code{I1_0}, \code{I2_0}, \code{S2_0}, \code{K_0}, \code{F_0}) of the fitted parameter
vector. Used to carry hidden-state estimates forward between successive
fitting windows in \code{prediction_by_window()} / \code{simulate_yearly_prediction()}.
}
\keyword{internal}
