% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{generate_rw_sd}
\alias{generate_rw_sd}
\title{Generate a \code{pomp::rw_sd} specification from a model file}
\usage{
generate_rw_sd(
  reg_rw,
  ivp_rw,
  model = NULL,
  model_dir = "dynamical_model/fitting",
  model_file = NULL,
  extra_rw = NULL,
  profile_var = NULL,
  remove_delta = TRUE
)
}
\arguments{
\item{reg_rw}{Numeric. Default/random-walk standard deviation to apply to
regular parameters (e.g. 0.03).}

\item{ivp_rw}{Numeric. Standard deviation to apply to initial condition
parameters (used inside an \code{ivp(...)} call, e.g. 0.1).}

\item{model}{Optional character. Short model name used to find
\verb{pomp_object_<model>.R} in \code{model_dir}. Ignored if \code{model_file} is
provided.}

\item{model_dir}{Character. Directory where model files live (default:
\code{dynamical_model/fitting}).}

\item{model_file}{Optional character. Absolute path to a model file. If
provided this is used instead of \code{model}/\code{model_dir}.}

\item{extra_rw}{Optional named vector or list of overrides. Names are
parameter names. Values may be numeric (regular rw sd) or language/call
objects (for example \code{call("ivp", 0.05)}). Setting a value to \code{NA}
removes the corresponding rw entry.}

\item{profile_var}{Optional character or vector of parameter names to
exclude from the returned \code{rw.sd} (useful when profiling parameters).}

\item{remove_delta}{Logical. If \code{TRUE} remove \code{delta} from the rw list (we
generally do not fit human mortality in these models). Default: \code{TRUE}.}
}
\value{
An object of class \code{rw.sd} (as produced by \code{pomp::rw_sd}).
}
\description{
Discover model parameter names from a \verb{pomp_object_<model>.R} file (or an
explicit \code{model_file}) and construct a \code{pomp::rw_sd} object programmatically.
}
\details{
The function creates regular random-walk standard-deviation entries for
estimated parameters (\code{par_names}) and \code{ivp(...)} entries for initial
condition parameters (\code{vp_names}). Call objects (language) are used for
\code{ivp(...)} entries so no \code{eval(parse(...))} is required.
}
