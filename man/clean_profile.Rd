% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_profile_results.R
\name{clean_profile}
\alias{clean_profile}
\title{Combine per-parameter profile-likelihood CSVs into one cleaned data frame}
\usage{
clean_profile(prefix, sufix, max_loglik = -500)
}
\arguments{
\item{prefix}{Character scalar. Path prefix shared by all per-parameter
CSV files, e.g. \code{"aggregated_results/profile_Ahmedabad_"}.}

\item{sufix}{Character scalar. Path suffix shared by all per-parameter
CSV files, e.g. \code{"_1997_2011.csv"}.}

\item{max_loglik}{Numeric scalar. Rows with \code{loglik >= max_loglik} are
dropped (default \code{-500}).}
}
\value{
A combined data frame with one row per accepted parameter set
across all profiled parameters.
}
\description{
Reads a set of per-parameter profile-likelihood result files (one CSV per
profiled parameter, all sharing a common \code{prefix}/\code{sufix} naming scheme),
drops rows \code{readr::read_csv()} flagged as parsing problems, coerces every
column to numeric, and keeps only fits above the \code{max_loglik} threshold.
The set of profiled parameter names is inferred from the columns of the
first file (for \code{"b1"}), so all sibling files are expected to share the
same non-parameter columns (\code{loglik}, \code{loglik.se}, \code{sample}, \code{flag},
\code{delta}).
}
