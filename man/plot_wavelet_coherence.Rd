% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_wavelet.R
\name{plot_wavelet_coherence}
\alias{plot_wavelet_coherence}
\title{Plot a cross-wavelet power or wavelet coherence spectrum}
\usage{
plot_wavelet_coherence(wc_list, which.image = "wp")
}
\arguments{
\item{wc_list}{Either a single coherency object as returned by
\code{WaveletComp::analyze.coherency()}, an unnamed list of such objects, or a
named list whose elements are each a list of such objects. The shape is
detected the same way as in \code{\link[=plot_wavelet]{plot_wavelet()}} (via \code{vctrs::vec_depth()}):
depth 3 is a single object (plotted unfaceted), depth 4 is an unnamed
list of objects (combined with \code{facet_grid(. ~ series)}), and depth 5 is
a named list of lists of objects (combined with
\code{facet_grid(group ~ series)}). Unlike \code{\link[=plot_wavelet]{plot_wavelet()}}, other shapes are
not explicitly rejected; they will fail when the plot is built from the
(empty) combined result.}

\item{which.image}{Character scalar selecting which quantity to plot when
\code{wc_list} has depth 4 or 5 (i.e. holds more than one coherency object):
\code{"wp"} (default) uses cross-wavelet power (\code{Power.xy}/\code{Power.xy.pval});
\code{"wc"} uses wavelet coherence (\code{Coherence}/\code{Coherence.pval}). It also
sets the fill legend title (\code{"Power"} vs \code{"Coherence"}). It has no effect
when \code{wc_list} is a single coherency object (depth 3), which is always
plotted as cross-wavelet power.}
}
\value{
A single ggplot object.
}
\description{
Build a ggplot raster of cross-wavelet power or wavelet coherence over
time and period from one or more \code{WaveletComp::analyze.coherency()}
results, overlaid with phase-difference arrows, a significance contour
(p = 0.1), and cone-of-influence shading. As in \code{\link[=plot_wavelet]{plot_wavelet()}}, several
coherency objects are combined into a single faceted plot rather than a
list of separate plots.
}
\details{
Phase-difference arrows are drawn from the coherency object's \code{sAngle}
component via \code{metR::geom_arrow()}. Before plotting, the angle values are
thinned (every 3rd date, every 6th period) and filtered to points where
power is positive and the p-value is at most 0.05, to keep the arrow
overlay legible.
}
