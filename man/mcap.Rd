% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcap.R
\name{mcap}
\alias{mcap}
\title{Compute a Monte Carlo Adjusted Profile (MCAP) confidence interval}
\usage{
mcap(lp, parameter, confidence = 0.95, lambda = 0.75, Ngrid = 1000)
}
\arguments{
\item{lp}{Numeric vector of profile log-likelihood values.}

\item{parameter}{Numeric vector of parameter values corresponding to
\code{lp} (same length).}

\item{confidence}{Confidence level for the interval.}

\item{lambda}{Loess span, and fraction of points (nearest to the
smoothed maximum) used to weight the local quadratic fit.}

\item{Ngrid}{Number of points in the grid used to evaluate the loess
smooth and locate the confidence interval.}
}
\value{
A list with elements: \code{lp}, \code{parameter}, \code{confidence} (the
inputs); \code{quadratic_fit} (the weighted local \code{lm} fit); \code{quadratic_max}
(vertex of the quadratic fit); \code{smooth_fit} (the \code{loess} object);
\code{fit} (data frame with columns \code{parameter}, \code{smoothed}, \code{quadratic}
giving the loess and quadratic predictions on the grid); \code{mle}
(parameter value maximizing the smoothed profile); \code{ci} (two-element
vector, the MCAP confidence interval); \code{delta} (the log-likelihood
drop used as the CI cutoff); \code{se_stat}, \code{se_mc}, \code{se} (statistical,
Monte Carlo, and total standard errors of the estimate).
}
\description{
Implements the MCAP method (Ionides et al.) for turning a noisy
Monte-Carlo profile log-likelihood into a smoothed profile and a
confidence interval for one parameter. The profile is smoothed with
loess, a weighted local quadratic is fit near the smoothed maximum, and
the resulting curvature is used to combine statistical and Monte-Carlo
(simulation) uncertainty into an inflated log-likelihood cutoff.
}
\details{
The local quadratic is parameterized as
\code{lp = c - a * parameter^2 + b * parameter}, so its curvature \code{a} and
the sampling variance of \code{a} and \code{b} are used to estimate both the
Monte Carlo variance (\code{se_mc}) coming from simulation noise in \code{lp}
and the usual statistical variance (\code{se_stat}). \code{delta} inflates the
classical \code{qchisq(confidence, df = 1) / 2} cutoff by a term
proportional to \code{se_mc} so the resulting \code{ci} accounts for both
sources of uncertainty.
}
