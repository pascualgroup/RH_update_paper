% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcap.R
\name{mcap_checked}
\alias{mcap_checked}
\title{Compute an MCAP profile with diagnostics and safeguards against degenerate profiles}
\usage{
mcap_checked(
  lp,
  parameter,
  confidence = 0.95,
  lambda = 0.75,
  Ngrid = 1000,
  external_mle = NULL,
  edge_fraction = 0.05,
  min_local_unique = 5
)
}
\arguments{
\item{lp}{Numeric vector of profile log-likelihood values.}

\item{parameter}{Numeric vector of parameter values corresponding to
\code{lp} (same length).}

\item{confidence}{Confidence level for the interval.}

\item{lambda}{Loess span, and (for the local quadratic fit) target
fraction of points nearest the smoothed maximum to include.}

\item{Ngrid}{Number of points in the grid used to evaluate the loess
smooth and locate the confidence interval.}

\item{external_mle}{Optional externally-known MLE for \code{parameter}. If
supplied, its smoothed log-likelihood is compared to the profile's
smoothed maximum and the gap is reported as \code{mle_gap}, as a
diagnostic of agreement between the profile and an independently
obtained MLE.}

\item{edge_fraction}{Fraction of the observed \code{parameter} range treated
as "near the boundary"; used to flag (\code{peak_at_edge}) when the
smoothed maximum falls within this margin of either end.}

\item{min_local_unique}{Minimum number of distinct \code{parameter} values
required within the local quadratic-fit neighborhood; below this,
\code{status} includes \code{"few_local_x"}.}
}
\value{
A list with the same core elements as \code{\link[=mcap]{mcap()}} (\code{lp},
\code{parameter}, \code{confidence}, \code{quadratic_fit}, \code{quadratic_max},
\code{smooth_fit}, \code{fit}, \code{mle}, \code{ci}, \code{delta}, \code{se_stat}, \code{se_mc}, \code{se}),
plus diagnostic fields: \code{quadratic_stationary} (the quadratic fit's
vertex regardless of concavity; \code{quadratic_max} is \code{NA} unless the fit
is concave), \code{a}, \code{b} (quadratic fit coefficients), \code{concave}
(logical), \code{ci_valid} (\code{FALSE} when the quadratic fit is not concave,
in which case \code{ci} is only the explored \code{parameter} range rather than
a true MCAP interval), \code{ci_truncated} (named logical vector with
elements \code{lower}/\code{upper}, flagging sides where the log-likelihood
cutoff was never crossed within the explored grid, so the edge of the
explored range was used instead), \code{ci_type} (one of \code{"mcap"},
\code{"mcap_beyond_lower_range"}, \code{"mcap_beyond_upper_range"},
\code{"mcap_beyond_both_ranges"}, or \code{"explored_range"}), \code{explored_range}
(range of the valid grid points), \code{peak_at_edge}, \code{n_unique_local},
\code{mle_gap}, and \code{status} (character vector of diagnostic flags such as
\code{"non_concave"}, \code{"peak_at_edge"}, \code{"few_local_x"},
\code{"left_unbounded"}, \code{"right_unbounded"}, or \code{"ok"}).
}
\description{
A defensive variant of \code{\link[=mcap]{mcap()}} that sanitizes its inputs and adds
diagnostics so that pathological profiles (a non-concave local quadratic
fit, a maximum at the edge of the explored range, too few distinct
parameter values near the maximum, or a confidence cutoff never crossed
within the explored grid) are reported explicitly via a \code{status} flag
vector instead of silently producing a nonsensical or infinite-bounded
confidence interval.
}
\details{
Non-finite \verb{(lp, parameter)} pairs are dropped up front. The
local quadratic is parameterized as
\code{lp = c - a * parameter^2 + b * parameter}, so \code{a > 0} is required for
it to be concave (a genuine local maximum) and for the MCAP variance
and cutoff formulas to be valid. If the fit is not concave, the
function returns early: \code{ci} is set to the explored \code{parameter} range,
\code{ci_valid} is \code{FALSE}, and \code{status} includes \code{"non_concave"}. If the
fit is concave, \code{delta} is computed the same way as in \code{\link[=mcap]{mcap()}}
(MC-adjusted cutoff). The function then checks, separately on each
side of the maximum, whether the log-likelihood actually drops below
\code{delta} before the edge of the evaluated grid is reached; if not, that
side of \code{ci} is set to the edge of the explored range instead of being
left unbounded, and is flagged via \code{ci_truncated}/\code{status}
(\code{"left_unbounded"}/\code{"right_unbounded"}) and reflected in \code{ci_type}.
}
