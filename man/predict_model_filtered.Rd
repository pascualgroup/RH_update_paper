% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{predict_model_filtered}
\alias{predict_model_filtered}
\title{Particle-filtered (data-conditioned) hidden states over the full study period}
\usage{
predict_model_filtered(
  parameters,
  n_sims = 10,
  model = "inf_exponent",
  covariate = "hadISD",
  start_year = 1997,
  start_month = 1,
  end_year,
  end_month = 12,
  end_year_prediction = 2023,
  end_month_prediction = 12,
  window_start,
  window_end,
  dataset,
  actual_end_year = NULL,
  actual_end_month = NULL,
  extra_cov = NULL,
  Np = 1000
)
}
\arguments{
\item{parameters}{Named numeric vector or single-row data.frame/tibble
with parameter values.}

\item{n_sims}{Integer. Number of independent draws from the smoothing
distribution (see Details) — computationally expensive, since each draw
requires its own full \code{pomp::pfilter()} pass.}

\item{model}{Character. Short model name (used to locate
\verb{pomp_object_<model>.R}).}

\item{covariate}{Character. Covariate column name.}

\item{start_year, start_month}{Integers. Start of the full interval.}

\item{end_year, end_month}{Integers. Forwarded to
\code{preprocess_data_prediction()}; see Details for their role here.}

\item{end_year_prediction, end_month_prediction}{Integers. End of the full
interval that gets particle-filtered.}

\item{window_start, window_end}{Integers. Calendar months defining the
covariate averaging window.}

\item{dataset}{Path to the city's dataset CSV.}

\item{actual_end_year, actual_end_month}{Optional integers. Covariate
normalization anchor; defaults to \code{end_year}/\code{end_month} when \code{NULL}
(see Details).}

\item{extra_cov}{Optional character vector of additional covariate names.}

\item{Np}{Integer. Number of particles per \code{pfilter()} run.}
}
\value{
A data.frame with columns \code{.id} (draw index, or \code{"data"} for the
observed series), \code{time}, the state variables (\code{S1}, \code{E}, \code{I1}, \code{S2},
\code{I2}, \code{K}, \code{F}, \code{W}, \code{cases}), the covariate/season columns
(\code{season1}...\code{season6}, \code{covariate}) for the draw rows, and \code{PF} for
the \code{.id == "data"} rows.
}
\description{
Unlike \code{\link[=predict_model_states]{predict_model_states()}}/\code{\link[=predict_model]{predict_model()}}, which \emph{simulate} the
model forward from its initial-condition parameters (an unconditional
check on the fitted dynamics), this function reports hidden states drawn
from \code{pomp::pfilter()}'s smoothing distribution: at every time point, the
reported state values come from particles that have been reweighted
against the observed case counts throughout the whole period. There is no
training/prediction split — the covariate table's training and prediction
halves (from \code{\link[=preprocess_data_prediction]{preprocess_data_prediction()}}) are immediately recombined,
so the particle filter runs continuously across the full
\verb{[start_year, end_year_prediction]} interval.
}
\details{
\code{end_year}/\code{end_month} keep their \code{\link[=preprocess_data_prediction]{preprocess_data_prediction()}} role of
anchoring the covariate min/max normalization (via \code{actual_end_year},
which defaults to \code{end_year} when \code{NULL}) to a specific fit window, even
though the filter itself always covers the full interval regardless of
\code{end_year}'s value; pass the training-window end that was used when the
parameters were originally estimated, so the covariate scale matches what
they were fit against.

To obtain \code{n_sims} independent draws from the smoothing
distribution, \code{pomp::pfilter()} is run \code{n_sims} separate times (each with
\code{filter.traj = TRUE}), extracting one traced-back particle trajectory per
run via \code{pomp::filter_traj()}. This is the statistically correct way to
sample from the smoothing distribution (see \code{?pomp::filter_traj}) — it is
\emph{not} the same as running one \code{pfilter()} with \code{Np = n_sims} and reading
off \code{n_sims} particles cross-sectionally at each time, which would not
respect genealogy and would understate uncertainty (particle diversity
collapses under repeated resampling).
}
