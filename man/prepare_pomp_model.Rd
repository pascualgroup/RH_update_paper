% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{prepare_pomp_model}
\alias{prepare_pomp_model}
\title{Prepare a pomp object from a model definition file}
\usage{
prepare_pomp_model(
  model,
  param,
  dat_cov,
  dat_mal,
  model_dir = "dynamical_model/fitting",
  model_file = NULL,
  validate_only = FALSE
)
}
\arguments{
\item{model}{Character. Short model name used to find \verb{pomp_object_<model>.R}
inside \code{model_dir}. Ignored if \code{model_file} is provided.}

\item{param}{Named numeric vector of parameter starting values passed to
\code{pomp::pomp}.}

\item{dat_cov}{Data frame. Covariates table produced by \code{preprocess_data}.
Must contain \code{time} (numeric), \code{pop}, and \code{dpopdt} columns and at least two
timepoints.}

\item{dat_mal}{Data frame. Malaria observations. Must contain \code{time} and
\code{PF} columns.}

\item{model_dir}{Character. Directory where \verb{pomp_object_<model>.R} is
looked up (default: \code{dynamical_model/fitting}).}

\item{model_file}{Optional character. Absolute path to a model file. If
provided, \code{model}/\code{model_dir} are ignored.}

\item{validate_only}{Logical. If \code{TRUE} perform validations and return a
small list with \code{status = 'ok'} rather than building the full \code{pomp}
object. Useful for CI and dry runs.}
}
\value{
A \code{pomp} object when \code{validate_only = FALSE}, or a list with
\code{status = 'ok'} when \code{validate_only = TRUE}.
}
\description{
This helper loads a model definition file \verb{pomp_object_<model>.R} (or a provided
\code{model_file}) into a private environment, validates the presence of required
symbols, verifies the covariate / observation data contract, and constructs
a \code{pomp} object ready for fitting or simulation. The function avoids
polluting the global environment by sourcing the model file into an internal
environment and referencing objects explicitly from there.
}
\details{
The model file must define the following objects: \code{par_names},
\code{vp_names}, \code{simul_pf}, \code{rmeas_pf}, \code{dmeas_pf}, \code{initlz_pf}, \code{state_names},
and the transformation helpers \code{log_transf}, \code{logit_transf}, and
\code{barycentric_transf}.
}
