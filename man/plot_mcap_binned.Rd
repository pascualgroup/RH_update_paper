% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcap.R
\name{plot_mcap_binned}
\alias{plot_mcap_binned}
\title{Build an MCAP profile-likelihood plot from binned, thinned profile output}
\usage{
plot_mcap_binned(
  dataset,
  column_name,
  n_quantiles = 20,
  max_per_bin = 20,
  diff_LL = NULL,
  remove_outlier = TRUE,
  zscore = TRUE
)
}
\arguments{
\item{dataset}{A data frame with (at least) columns \code{loglik} and
\code{column_name}.}

\item{column_name}{Character scalar naming the parameter column to
profile over.}

\item{n_quantiles}{Number of quantile bins passed to
\code{\link[=bin_by_quantile]{bin_by_quantile()}}.}

\item{max_per_bin}{Maximum number of highest-\code{loglik} rows kept per
quantile bin.}

\item{diff_LL}{Optional numeric; if supplied, rows with \code{loglik} more
than \code{diff_LL} below the maximum \code{loglik} are dropped before binning.}

\item{remove_outlier}{Logical; whether to filter outlying values of
\code{column_name} after binning/thinning.}

\item{zscore}{Logical; if \code{TRUE} outliers are removed using an absolute
z-score threshold of 2, otherwise using 1.5*IQR fences.}
}
\value{
A ggplot object (from \code{\link[=mcap_plot]{mcap_plot()}}) with \code{column_name} as the
x-axis label and \code{"loglikelihood"} as the y-axis label.
}
\description{
Thins a raw profile-likelihood dataset (typically many Monte Carlo
replicates per parameter value) by binning \code{column_name} into quantiles
and keeping only the highest-loglik rows per bin, then fits \code{\link[=mcap]{mcap()}} to
the thinned data and plots the result with \code{\link[=mcap_plot]{mcap_plot()}}.
}
\details{
Pipeline: drop incomplete rows, optionally drop rows far below
the maximum \code{loglik} (\code{diff_LL}), bin by quantile of \code{column_name}
(\code{\link[=bin_by_quantile]{bin_by_quantile()}}), keep the top \code{max_per_bin} rows by \code{loglik} in
each bin, optionally remove outliers in \code{column_name}, then call
\code{\link[=mcap]{mcap()}} on the thinned \verb{(loglik, column_name)} pairs and plot the
result with \code{\link[=mcap_plot]{mcap_plot()}}.
}
