% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcap.R
\name{plot_simple_CI}
\alias{plot_simple_CI}
\title{Build a classical (non-MC-adjusted) profile-likelihood plot from binned output}
\usage{
plot_simple_CI(
  dataset,
  column_name,
  n_quantiles = 20,
  max_per_bin = 20,
  diff_LL = NULL,
  remove_outlier = TRUE,
  zscore = TRUE
)
}
\arguments{
\item{dataset}{A data frame with (at least) columns \code{loglik} and
\code{column_name}.}

\item{column_name}{Character scalar naming the parameter column to
profile over.}

\item{n_quantiles}{Number of quantile bins passed to
\code{\link[=bin_by_quantile]{bin_by_quantile()}}.}

\item{max_per_bin}{Maximum number of highest-\code{loglik} rows kept per
quantile bin.}

\item{diff_LL}{Optional numeric; if supplied, rows with \code{loglik} more
than \code{diff_LL} below the maximum \code{loglik} are dropped before outlier
removal and binning.}

\item{remove_outlier}{Logical; whether to filter outlying values of
\code{column_name} before binning.}

\item{zscore}{Logical; if \code{TRUE} outliers are removed using an absolute
z-score threshold of 2, otherwise using 1.5*IQR fences.}
}
\value{
A ggplot object (from \code{\link[=mcap_plot]{mcap_plot()}}) with \code{column_name} as the
x-axis label and \code{"loglikelihood"} as the y-axis label.
}
\description{
Same thinning-and-plotting idea as \code{\link[=plot_mcap_binned]{plot_mcap_binned()}}, but fits
\code{\link[=simple_mcap]{simple_mcap()}} instead of \code{\link[=mcap]{mcap()}}, and removes outliers in
\code{column_name} before binning rather than after.
}
\details{
Pipeline: drop incomplete rows, optionally drop rows far below
the maximum \code{loglik} (\code{diff_LL}), optionally remove outliers in
\code{column_name}, bin by quantile of \code{column_name}
(\code{\link[=bin_by_quantile]{bin_by_quantile()}}) and keep the top \code{max_per_bin} rows by \code{loglik}
in each bin, then call \code{\link[=simple_mcap]{simple_mcap()}} on the thinned pairs. The
resulting \code{delta} is then explicitly overwritten with
\code{qchisq(0.95, df = 1) / 2} (the classical 95\% cutoff regardless of
any other confidence level) before plotting with \code{\link[=mcap_plot]{mcap_plot()}}.
}
