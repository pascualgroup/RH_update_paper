% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_safe.R
\name{write_manifest_for_result}
\alias{write_manifest_for_result}
\title{Write a run manifest describing a result file}
\usage{
write_manifest_for_result(
  df,
  result_path,
  mode_label,
  manifest_info = NULL,
  dir = NULL
)
}
\arguments{
\item{df}{The result data frame written to \code{result_path}; used only to
determine \code{status} (\code{"finished"} if it has rows, \code{"empty"} otherwise).}

\item{result_path}{Path to the result CSV file the manifest describes.}

\item{mode_label}{Character scalar identifying which fitting mode produced
the result (recorded under \code{parameters$mode}).}

\item{manifest_info}{Optional list (e.g. from \code{\link[=create_manifest_info]{create_manifest_info()}}) to
reuse and annotate instead of building a manifest from scratch.}

\item{dir}{Optional directory to write the manifest into; defaults to
\code{dirname(result_path)}.}
}
\value{
Invisibly returns the path to the written manifest file, or \code{NULL}
if \code{df} or \code{result_path} is \code{NULL}.
}
\description{
Build (or update) a manifest object for a fitting/profiling result and
write it next to \code{result_path} via \code{\link[=safe_write_manifest_next_to]{safe_write_manifest_next_to()}}. If
\code{manifest_info} is supplied (e.g. the info object returned alongside a fit)
it is reused and annotated with the result file path, status, and
\code{mode_label}; otherwise a minimal manifest is constructed from scratch.
}
