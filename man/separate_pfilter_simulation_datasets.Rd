% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{separate_pfilter_simulation_datasets}
\alias{separate_pfilter_simulation_datasets}
\title{Split a dataset into sliding training/refit/simulation windows}
\usage{
separate_pfilter_simulation_datasets(
  dataset,
  start_year = 1997,
  start_month = 1,
  end_year = 2014,
  end_month = 12,
  end_year_prediction = 2019,
  end_month_prediction = 12,
  prediction_window
)
}
\arguments{
\item{dataset}{A data frame with \code{year} and \code{month} columns spanning at
least \code{start_year}-\code{start_month} through
\code{end_year_prediction}-\code{end_month_prediction}.}

\item{start_year}{Integer. First year of the training period.}

\item{start_month}{Integer. First month (in \code{start_year}) of the training period.}

\item{end_year}{Integer. Last year of the training period.}

\item{end_month}{Integer. Last month (in \code{end_year}) of the training period.}

\item{end_year_prediction}{Integer. Last year of the forecast horizon.}

\item{end_month_prediction}{Integer. Last month (in \code{end_year_prediction})
of the forecast horizon.}

\item{prediction_window}{Integer. Length, in years, of the sliding refit
window used to re-estimate parameters/states before each forecast year.}
}
\value{
A list with elements \code{training_df}, \code{pfilter_df},
\code{partial_pfilter_df}, and \code{simulation_df} as described above.
}
\description{
Helper for \code{prediction_by_window()} / \code{simulate_yearly_prediction()} that
partitions \code{dataset} into the pieces needed to walk forward one year at a
time from the end of training through \code{end_year_prediction}:
\itemize{
\item \code{training_df}: the full training period
(\code{start_year}-\code{start_month} to \code{end_year}-\code{end_month}).
\item \code{simulation_df}: one data frame per forecast year (each spanning
12 months), from the month after training through
\code{end_year_prediction}-\code{end_month_prediction}.
\item \code{pfilter_df}: one data frame per refit step, each spanning
\code{prediction_window} years ending the month before the corresponding
\code{simulation_df} entry begins (the very first entry instead starts at
\code{start_year}-\code{start_month}, growing/sliding the window as needed).
\item \code{partial_pfilter_df}: like \code{pfilter_df} but truncated to end
\code{prediction_window - 1} years earlier, i.e. a shorter partial window
ending one year ahead of the previous refit point.
}
}
\keyword{internal}
