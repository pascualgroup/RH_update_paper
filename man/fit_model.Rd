% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{fit_model}
\alias{fit_model}
\title{Fit a POMP model over a parameter grid or starting parameter vector}
\usage{
fit_model(
  parameters,
  city,
  model = "const",
  covariate = "hadISD_VC",
  start_year = 1997,
  start_month = 1,
  end_year = 2014,
  end_month = 12,
  window_start,
  window_end,
  dataset,
  mode = "fit",
  allow_parallel = TRUE,
  n_cores = 1,
  seed_num = as.integer(Sys.time()),
  start_index = 1,
  output_to_file = TRUE,
  output_result = NULL,
  output_log = NULL,
  output_format = "data.frame",
  profile_var = NULL,
  extra_cov = NULL,
  normalize_extra = TRUE,
  model_file = NULL,
  model_dir = "dynamical_model/fitting",
  extra_rw = NULL,
  validate_only = FALSE,
  reg_rw = NULL,
  ivp_rw = NULL,
  mif_control = NULL,
  pfilter_control = NULL,
  manifest_append = FALSE,
  manifest_path = NULL,
  verbose = FALSE
)
}
\arguments{
\item{parameters}{Named numeric vector or single-row data.frame/tibble with starting parameter values.}

\item{city}{Character. City name used to generate default output filenames.}

\item{model}{Character. Short model name (used to locate \verb{pomp_object_<model>.R} in \code{model_dir}).}

\item{covariate}{Character. Covariate column name to use (e.g. \code{'hadISD_VC'}).}

\item{start_year}{Integer. First year to include.}

\item{start_month}{Integer. Starting month in \code{start_year}.}

\item{end_year}{Integer. Last year to include.}

\item{end_month}{Integer. Final month to include.}

\item{mode}{Character. One of \code{'fit'}, \code{'refined'}, \code{'refined_second'} controlling default rw sizes.}

\item{allow_parallel}{Logical. Whether to allow parallel execution (via futures).}

\item{n_cores}{Integer. Number of cores/workers to request when parallel.}

\item{seed_num}{Integer. RNG seed for runs.}

\item{start_index}{Integer. Index offset for sample numbering (useful when running batched grids).}

\item{output_to_file}{Logical. Whether to append results to disk.}

\item{output_result}{Path to result CSV file. If missing a default is generated.}

\item{output_log}{Path to log file. If missing a default is generated.}

\item{output_format}{Character. \code{'data.frame'} or \code{'pomps'} for simulation outputs.}

\item{profile_var}{Optional character. Parameter name(s) to exclude from \code{rw.sd} (for profiling experiments).}

\item{extra_cov}{Optional character vector of additional covariate names to merge into the covariate dataset.}

\item{normalize_extra}{Logical. Whether to min-max normalize extra covariates.}

\item{model_file}{Optional character. Absolute path to a \code{pomp_object_*.R} model file (overrides \code{model} + \code{model_dir}).}

\item{extra_rw}{Optional named list or vector of rw overrides passed to \code{generate_rw_sd()} (names -> sd or call objects). Set a value to \code{NA} to remove that entry.}

\item{validate_only}{Logical. If \code{TRUE} perform validations and return early with \code{list(status = 'ok')} instead of constructing the full \code{pomp} object and fitting.}

\item{reg_rw}{Optional numeric. Random-walk SD to use for regular (non-ivp) parameters. If \code{NULL} defaults are chosen based on \code{mode}.}

\item{ivp_rw}{Optional numeric. Random-walk SD to use for initial-condition parameters. If \code{NULL} defaults are chosen based on \code{mode}.}

\item{mif_control}{Optional list of control arguments forwarded to \code{pomp::mif2} (e.g. \code{list(Np=1000, Nmif=50)}). If \code{NULL} defaults are used.}

\item{pfilter_control}{Optional list of control arguments forwarded to \code{pomp::pfilter} (e.g. \code{list(Nrep=10, Np=10000)}). If \code{NULL} defaults are used.}

\item{manifest_append}{Logical. If \code{TRUE} and a manifest file already exists at the target path, the new run info will be appended into an array; if \code{FALSE} the manifest file will be (re)created/overwritten. Default: \code{FALSE}.}

\item{scale}{Character. Aggregation scale: \code{'monthly'}, \code{'daily'}, or \code{'hourly'}.}

\item{dataset_mal}{Path to CSV with malaria case data.}

\item{dataset_VC}{Path to CSV with covariate data (VC/climate).}

\item{extra_cov_dataset}{Optional path to a CSV containing \code{extra_cov} columns (if not present in \code{dataset_VC}).}
}
\value{
A data.frame (or list) with fitted parameter estimates and log-likelihood information when \code{validate_only = FALSE}.
When \code{validate_only = TRUE} a small list with \code{status = 'ok'} and \code{model_file} is returned.
}
\description{
High-level wrapper that prepares data, constructs a \code{pomp} model from a
model definition file, creates random-walk specifications, and runs the
fitting routine. This function provides convenience wiring for experiments
and supports fast "validate-only" checks useful for CI.
}
