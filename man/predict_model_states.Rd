% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_functions.R
\name{predict_model_states}
\alias{predict_model_states}
\title{Simulate a training period and forecast forward, including hidden states}
\usage{
predict_model_states(
  parameters,
  city,
  n_sims = 1,
  model = "original",
  covariate = "RH2",
  start_year = 1997,
  start_month = 1,
  end_year = 2014,
  end_month = 12,
  end_year_prediction = 2019,
  end_month_prediction = 12,
  window_start,
  window_end,
  dataset,
  output_format = c("data.frame", "pomps"),
  actual_end_year = NULL,
  actual_end_month = NULL,
  extra_cov = NULL
)
}
\arguments{
\item{parameters}{Named numeric vector or single-row data.frame/tibble
with parameter values (used for both the training simulation and as the
fixed non-state parameters of the prediction model).}

\item{city}{The name of the city (currently unused inside the function
body, kept for interface consistency with related functions).}

\item{n_sims}{The number of simulations/particles to use (default is 1).}

\item{model}{The model to use (default is "original").}

\item{covariate}{The covariate to use (default is "RH2").}

\item{start_year}{The starting year of the training period (default is 1997).}

\item{start_month}{The starting month of the training period (default is 1).}

\item{end_year}{The ending year of the training period (default is 2014).}

\item{end_month}{The ending month of the training period (default is 12).}

\item{end_year_prediction}{The ending year of the forecast period (default is 2019).}

\item{end_month_prediction}{The ending month of the forecast period (default is 12).}

\item{window_start}{The starting month of the window used to aggregate
the covariate within each year.}

\item{window_end}{The ending month of the window used to aggregate the
covariate within each year.}

\item{dataset}{The path to the dataset CSV file.}

\item{output_format}{The format of the output (default is "data.frame").
Currently validated but not forwarded to the internal \code{simulate()}
calls, which always request \code{"data.frame"}.}

\item{actual_end_year}{Optional. End year used for covariate
normalization range in \code{preprocess_data_prediction()}; defaults to
\code{end_year} when \code{NULL}.}

\item{actual_end_month}{Optional. End month used for covariate
normalization range in \code{preprocess_data_prediction()}; defaults to
\code{end_month} when \code{NULL}.}

\item{extra_cov}{Optional data frame of additional covariates, passed
through to \code{preprocess_data_prediction()}.}
}
\value{
A tibble combining the training and prediction simulations, with
columns including \code{time}, \code{.id}, \code{is_data} (\code{"data"}/\code{"simulation"}),
\code{type} (\code{"data"}/\code{"training"}/\code{"prediction"}), the observation and
covariate columns, and the hidden compartment states (e.g. \code{S1}, \code{E},
\code{I1}, \code{I2}, \code{S2}, \code{K}, \code{F}).
}
\description{
Loads and preprocesses \code{dataset} via \code{preprocess_data_prediction()}, then:
(1) simulates the training period with the supplied \code{parameters}; (2)
particle-filters the trained model over the training window
(\code{pomp::pfilter(..., save.states = TRUE)}) to obtain posterior estimates
of the hidden compartment states (and the covariate-driving states \code{K},
\code{F}) at the end of training, normalizing the epidemiological compartments
(\code{S1}, \code{E}, \code{I1}, \code{I2}, \code{S2}) to fractions; (3) builds a fresh \code{pomp}
object over the prediction period only, seeds it with the particle-filter
estimated initial states (one column per particle) via \code{pomp::parmat()},
sets its \code{t0} to the end of the training period, and simulates forward.
Training and prediction simulations are row-bound and labeled, and the
covariate/season columns (which \code{pomp::simulate()} can misalign across
\code{.id} when \code{params} is a matrix) are re-derived from the known covariate
series by joining on \code{time} rather than trusting the simulate() output.
}
